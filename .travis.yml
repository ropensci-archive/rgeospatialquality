# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: false
addons:
  apt:
    packages:
      - libv8-dev
r_github_packages:
  - jimhester/covr
after_success:
  - Rscript -e 'library(covr);codecov()'
